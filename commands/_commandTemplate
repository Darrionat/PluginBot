module.exports = { 
    name: "commandname", //self explanatory
    description: "somecommanddescription", //self explanatory
    aliases: [], // how else can we trigger
    guild: ["all"], // "all" or a guild id required 
    nsfw: false, //boobies or nah?
    user_permissions: [], //perms the user needs to run command
    bot_permissions: [], //perms we need to process the command
    args_required: 0, //number of arguements we require from the user
    args_usage: "", //what these arguements should be [optional] <required>
    cooldown: 5, // cooldown in seconds

    async execute(client, message, args) {
        //code go here
    }
};